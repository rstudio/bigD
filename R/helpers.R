#' @export
fdf <- function(format_name) {

  # Create a character object
  x <- format_name

  class(x) <- "fdf"
  x
}

#' @export
ftf_12 <- function(format_name) {

  # Create a character object
  x <- format_name

  class(x) <- "ftf_12"
  x
}

#' @export
ftf_24 <- function(format_name) {

  # Create a character object
  x <- format_name

  class(x) <- "ftf_24"
  x
}
